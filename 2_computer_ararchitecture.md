# 컴퓨터 구조

## 1. 컴퓨터 구조의 큰 그림

### 컴퓨터가 이해하는 정보
- 컴퓨터는 오직 **0과 1**, 즉 **이진수(Binary)** 로 정보를 처리함  
- 모든 **데이터**(숫자, 문자, 이미지 등)와 **명령어**는 이진수로 변환되어 해석됨

### 컴퓨터의 핵심 부품
컴퓨터는 다음의 네 가지 주요 부품으로 구성됨

- **CPU(중앙처리장치)**: 연산과 제어 담당  
- **메모리(RAM)**: 실행 중인 프로그램 데이터를 임시로 저장  
- **보조기억장치(HDD, SSD 등)**: 데이터를 영구적으로 저장  
- **입출력장치**: 사용자와 컴퓨터 간 정보 교환을 담당  

※ **캐시 메모리**는 CPU가 메모리에 더 빠르게 접근할 수 있도록 도와주는 고속 임시 저장소로 활용됨

이 부품들은 **시스템 버스**(주소 버스, 데이터 버스, 제어 버스)를 통해 상호 연결되어 데이터를 주고받음

---

## 2. 컴퓨터가 이해하는 정보

### 숫자 표현
- 모든 숫자는 **2진수**로 변환되어 저장 및 연산됨  
  예: 10진수 5 → 2진수 `101`
- **워드(word)**는 CPU가 한 번에 처리할 수 있는 데이터의 크기를 의미함
- **부동소수점**은 실수를 **가수 + 지수** 형태로 나누어 표현함으로써 효율적으로 저장함

### 문자 표현
- 문자는 **인코딩 방식**(ASCII, 유니코드 등)을 통해 이진수로 저장됨
- ASCII는 영어 기반 문자를 표현하고, 유니코드는 다양한 언어와 특수문자까지 지원함

### 명령어 구성
- **명령어**는 컴퓨터가 수행할 동작을 나타내는 이진 코드로 구성됨
- 구성 요소  
  - **연산 코드(opcode)**: 수행할 동작 지정  
  - **오퍼랜드(operand)**: 데이터 또는 주소 지정  
- 하나의 명령어는 연산 코드와 0개 이상의 오퍼랜드로 구성됨

### 명령어 사이클 (Instruction Cycle)
- CPU가 명령어를 처리하는 과정은 **인출(Fetch)**과 **실행(Execute)** 단계로 구성됨

**인출(Fetch)**  
1. 프로그램 카운터(PC)의 주소를 메모리 주소 레지스터(MAR)로 전달  
2. 해당 주소의 명령어를 메모리에서 읽어 메모리 버퍼 레지스터(MBR)에 저장  
3. 명령어를 명령어 레지스터(IR)로 이동, PC는 다음 명령어로 증가

**실행(Execute)**  
- IR의 명령어를 해석하여 연산, 분기, 메모리 접근 등의 작업을 수행  
- 명령어 사이클은 반복되며, 이 과정을 통해 전체 프로그램이 실행됨

---

## 3. CPU

### 레지스터
- CPU 내부의 **초고속 임시 저장소**로, 연산에 필요한 데이터나 주소 등을 저장함  
- 명령어 실행 속도에 직접적인 영향을 줌

### 인터럽트
- 입출력 완료, 예외 발생 등 이벤트 발생 시 현재 작업을 일시 중단하고  
  우선 처리해야 할 작업을 먼저 수행함

### CPU 성능 향상 기술
- **파이프라이닝**: 명령어를 여러 단계로 분할하여 **동시 처리**함으로써 처리 속도를 높임  
- **병렬 처리**: 여러 명령어를 동시에 실행함으로써 전체 처리량을 증가시킴  
- 이 외에도 슈퍼스칼라, 분기 예측, out-of-order 실행 등의 방식이 사용됨

---

## 4. 메모리

### RAM
- 프로그램 실행 중 데이터를 임시로 저장하는 **휘발성 메모리**  
- 전원이 꺼지면 저장된 데이터는 모두 사라짐

### 엔디안(Endian)
- 데이터를 메모리에 저장할 때의 **바이트 순서 방식**을 의미함  
  - **빅 엔디안**: 상위 바이트를 먼저 저장  
  - **리틀 엔디안**: 하위 바이트를 먼저 저장

### 캐시 메모리
- CPU와 RAM 사이에 위치하며 **자주 사용되는 데이터**를 저장함  
- 메모리 접근 속도를 높이는 데 사용됨

---

## 5. 보조기억장치와 입출력장치

### 보조기억장치
- 데이터를 영구적으로 저장하는 장치  
  예: HDD, SSD, CD-ROM 등  
- **RAID**는 여러 디스크를 결합하여 성능 향상과 데이터 신뢰성을 확보함

### 입출력장치
- 컴퓨터 외부와 내부 간 정보를 주고받는 장치  
  - 입력장치: 키보드, 마우스 등  
  - 출력장치: 모니터, 프린터 등

### 입출력 기법
- 입출력 장치와 CPU 간의 효율적인 데이터 교환을 위한 방식  
  - 프로그램 직접 제어 방식  
  - 인터럽트 기반 제어  
  - **DMA(Direct Memory Access)** 방식

---

## 추가: GPU의 용도와 처리 방식

- **GPU(Graphics Processing Unit)**는 대량의 데이터를 병렬로 처리하는 데 최적화된 장치  
- 그래픽 연산뿐 아니라 **AI, 병렬 계산, 과학 시뮬레이션** 등에서도 활용됨