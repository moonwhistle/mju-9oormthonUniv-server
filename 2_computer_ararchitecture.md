
<질문 목록>
Q1. 컴퓨터가 데이터를 처리하고 프로그램을 실행하는 전체 과정을 간단히 설명하세요
A1.
컴퓨터는 입력 장치로부터 데이터를 받아 저장장치에 보관된 프로그램을 메모리(RAM)로 로드하고, 중앙처리장치(CPU)가 이를 처리합니다. 
CPU는 명령어를 하나씩 가져와 해석하고 실행(fetch-decode-execute)하며, 이때 메모리, 레지스터, ALU 등을 이용해 연산을 수행하고 결과를 저장하거나 출력 장치를 통해 전달합니다.
전체 과정은 입력(Input) → 처리(Process) → 출력(Output)의 흐름을 따릅니다.

Q2. RAM과 보조기억장치(하드디스크, SSD)의 차이는 무엇인가요?
A2.
RAM은 휘발성 저장장치로, 전원이 꺼지면 저장된 데이터가 모두 사라집니다. 반면, 하드디스크나 SSD와 같은 보조기억장치는 비휘발성 저장장치로, 전원이 꺼져도 데이터가 유지됩니다. RAM은 CPU가 실행할 프로그램과 데이터를 일시적으로 저장하는 용도이고, 보조기억장치는 장기적으로 데이터를 보관하는 용도입니다

Q3. 비동기 인터럽트와 동기 인터럽트인 예외(Exception)의 차이는 무엇인가요?
A3. 
비동기 인터럽트는 CPU 외부에서 발생하며 비동기적으로 처리됩니다. 예: 키보드 입력, 디스크 I/O
동기 인터럽트, 예외는 프로그램 실행 중 CPU 내부에서 발생합니다. 예: 0으로 나누기, 페이지 폴트
둘 다 인터럽트 처리 루틴을 호출하지만, 발생 원인과 시점이 다릅니다.

Q4. CPU가 하드웨어 인터럽트(비동기 인터럽트)를 처리하는 순서를 간단하게 설명하세요
A4.
하드웨어 인터럽트는 외부 장치(키보드, 타이머 등)가 CPU에 처리 요청을 보내는 비동기적 신호입니다. CPU는 다음과 같은 과정을 통해 인터럽트를 처리합니다.

1) 인터럽트 요청 (Interrupt Request, IRQ) : 장치 컨트롤러가 인터럽트 요청 신호 (IRQ)를 CPU에 보냅니다.
2) 인터럽트 허용 여부 확인
CPU는 인터럽트 플래그(Interrupt Enable Flag)를 확인하여, 인터럽트를 수락할 수 있는 상태인지 판단합니다.
3) 현재 작업 저장 (Context Save)
현재 실행 중인 명령어가 끝나면, CPU는 프로그램 카운터(PC), 레지스터 값 등 현재 상태를 스택에 저장하여 작업 상태를 보존합니다.
4) 인터럽트 벡터 테이블 참조
인터럽트 신호의 번호(ID)에 따라 인터럽트 벡터 테이블(IVT)에서 해당 ISR(Interrupt Service Routine)의 주소를 조회합니다.
5) ISR(인터럽트 서비스 루틴) 실행
CPU는 해당 ISR로 분기하여, 인터럽트 요청을 처리하는 코드(예: 키 입력 처리)를 실행합니다.
6) 복구 및 원래 작업 복귀
ISR 실행이 끝나면, 저장해 두었던 PC와 레지스터 값을 복원하고, 중단된 명령어 다음 명령어부터 다시 실행을 계속합니다.

Q5. 인터럽트와 폴링의 차이를 설명하세요
A5. 
인터럽트는 장치가 준비되었을 때 CPU에 신호를 보내 처리 요청을 하는 방식이고, 폴링은 CPU가 주기적으로 장치 상태를 직접 확인하는 방식입니다.

Q6. 파이프라이닝이 CPU 성능 향상에 실패하는 경우 3가지를 간단하게 설명하세요
A6.
파이프라이닝은 명령어를 동시에 처리해 성능을 높이지만, 명령어들 사이에 데이터가 서로 필요하거나(데이터 해저드), 분기 명령어로 인해 다음 명령어가 확실하지 않거나(제어 해저드), 하드웨어 자원이 부족한 경우(구조적 해저드)에는 파이프라인이 멈추거나 비워져서 성능이 오히려 떨어질 수 있습니다.

Q7. 빅 엔디안과 리틀 엔디안의 차이는 무엇인가요?
A7. 
빅 엔디안은 데이터를 저장할 때 가장 중요한 바이트(상위 바이트)를 가장 앞에 저장하는 방식이고, 
리틀 엔디안은 가장 덜 중요한 바이트(하위 바이트)를 가장 앞에 저장하는 방식입니다. CPU 아키텍처에 따라 사용하는 방식이 다릅니다.

Q8.캐시 메모리는 무엇이며, 어떤 원리로 데이터를 저장하나요?
A8.
캐시 메모리는 CPU와 메인 메모리 사이에 위치한 고속 임시 저장장치로, CPU가 자주 접근하는 데이터를 미리 저장하여 전체 시스템의 처리 속도를 향상시키는 역할을 합니다.
캐시 메모리는 시간적 지역성(최근에 사용한 데이터는 곧 다시 사용될 가능성이 높음)과 공간적 지역성(현재 접근한 주소 근처의 데이터도 곧 접근될 가능성이 높음)이라는 두 가지 원칙을 기반으로 데이터를 저장합니다.
이러한 원칙 덕분에 CPU는 자주 사용하는 명령어와 데이터를 메모리 대신 캐시에서 빠르게 읽을 수 있으며, 메모리 접근 횟수를 줄여 성능을 높일 수 있습니다.

Q9. 메모리 계층 구조에서 캐시 미스가 발생했을 때, 어떤 과정으로 데이터가 CPU에 전달되는지 설명하세요.
A9. 
캐시 미스가 발생하면
1) CPU는 L1 캐시에서 데이터를 찾지 못합니다.
2) L2 캐시를 탐색하고, 없으면 L3 캐시까지 탐색합니다.
3) 모든 캐시에서 데이터를 찾지 못하면, 메인 메모리(RAM)에서 데이터를 읽어옵니다.
4) 데이터가 메인 메모리에도 없으면, 보조기억장치(디스크 등)에서 데이터를 로드합니다.
5) 데이터를 찾은 후, 해당 데이터를 캐시에 저장하고 CPU에 전달합니다.
이 과정에서 계층이 내려갈수록 접근 속도가 느려지므로, 캐시 미스가 잦으면 시스템 성능이 크게 저하됩니다.

-------- 심화 질문 --------
Q10. A 컴퓨터에서 동작하던 프로그램이 B 컴퓨터에선 훨씬 느리게 실행됩니다. CPU 클럭은 B가 더 빠른데도요. 가능한 이유는?
A10.
클럭 속도만으로 성능을 판단할 수는 없습니다. 캐시 크기, CPI, 파이프라인 구조, 메모리 접근 속도 등이 전체 성능에 영향을 미칩니다. 특히 캐시 미스가 많거나 분기 예측 실패율이 높으면 실행 속도는 느려질 수 있습니다.

Q11. 명령어 파이프라이닝을 더 많이 단계로 나누면 항상 더 빠를까요?
A11.
단계 수가 많아질수록 해저드 해결이 어려워지고, 분기 예측 실패 시 손실되는 클록 사이클이 늘어나므로 오히려 성능이 저하될 수 있습니다. 예측률이 낮거나 명령어 간 의존도가 높을수록 오버헤드가 커집니다.

Q12. 멀티코어 CPU에서 각 코어가 독립적인 L1 캐시만 가지고, L2/L3 캐시를 공유하지 않는다면 어떤 장단점이 생길까요?
A12.
장점은 각 코어의 데이터 접근 속도가 빨라지고, 캐시 일관성 관리가 단순해집니다. 
단점은 여러 코어가 동일 데이터를 사용할 때 데이터 일관성 문제가 심해지고, 데이터 중복 저장으로 전체 캐시 효율이 떨어질 수 있습니다.

Q13. 동일한 명령어 집합을 사용하는 두 CPU가 있는데, 한 쪽은 파이프라이닝을 지원하고 한 쪽은 지원하지 않습니다. 그런데 짧은 루프를 반복하는 프로그램에서는 둘의 성능 차이가 거의 없습니다. 왜 그럴까요?
A13.
짧은 루프에서는 파이프라인이 채워지기도 전에 루프가 끝나거나, 파이프라인의 초기화(플러시)가 반복적으로 발생해 파이프라이닝의 이점이 사라질 수 있습니다.